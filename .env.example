NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-side only (no NEXT_PUBLIC_ prefix). Bypasses RLS entirely — never
# expose this to the browser. See src/lib/supabaseAdminClient.ts.
SUPABASE_SERVICE_ROLE_KEY=
